# Plausible Analytics Configuration
# 你的网站域名（在 Plausible 中注册的域名）
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=lensmakerformula.vercel.app

# Plausible API Host (自建实例的域名，如果使用官方服务则留空)
# 例如：analytics.yourdomain.com 或 plausible.yourdomain.com
NEXT_PUBLIC_PLAUSIBLE_API_HOST=plausible.wulongmaqiduo.win

# 如果使用官方 Plausible.io 服务，只需要设置 NEXT_PUBLIC_PLAUSIBLE_DOMAIN
# 如果使用自建实例，需要同时设置 NEXT_PUBLIC_PLAUSIBLE_API_HOST


